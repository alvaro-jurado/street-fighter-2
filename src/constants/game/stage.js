// Altura del suelo en el escenario
export const STAGE_FLOOR = 216;

// Ancho total del escenario
export const STAGE_WIDTH = 768;

// Altura total del escenario
export const STAGE_HEIGHT = 256;

// Punto medio horizontal del escenario
export const STAGE_MID_POINT = STAGE_WIDTH / 2;

// Espaciado adicional alrededor del escenario para permitir el desplazamiento
export const STAGE_PADDING = 256;

// Límite de desplazamiento antes de que se active el desplazamiento automático
export const SCROLL_BOUNDS = 100;
